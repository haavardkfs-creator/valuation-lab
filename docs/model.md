# The Finance Model

This file is the single source of truth for the valuation model, written in plain words:
every formula, every assumption, and where each input comes from.

It is empty for now. Haavard fills it in during Phase 1, after building the Excel model.
Claude never changes a formula in this file without asking Haavard first.
